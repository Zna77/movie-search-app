window.APP_CONFIG = {
  // Optional: set to your Vercel domain for live-server usage.
  // Example: "https://your-app.vercel.app"
  apiBase: "",
  // Optional: direct TMDB key for local static usage (not recommended for production).
  tmdbApiKey: "",
};
